# Cloudflare Operator values (spec §7.2). Token + accountId come from the secret
# applied separately so we never commit credentials.
config:
  apiToken:
    secretName: cloudflare-api-token
    secretKey: token
  accountId: 366698543affcfd96a36459d76770fc4

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

nodeSelector:
  workload: compute
