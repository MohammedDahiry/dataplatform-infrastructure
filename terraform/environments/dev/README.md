# Dev Environment

Live Terraform environment for the landing zone in `us-east-1`.

Before first use:

1. Run `terraform/bootstrap` once.
2. Replace placeholders in `backend.tf`.
3. Run plan/apply through CI.
